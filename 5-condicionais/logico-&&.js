// operadorres logicos &&
// && só da verdadeiro se todas fortem verdadeiras
const temIngresso = true;
const censura = 16;
const idade = 14;

if (temIngresso === true && idade >= censura) {
    console.log("Pode entrar")
} else {
    console.log("Barrada")
}
//fazer só se não interessar por uqla motivo passou ou não