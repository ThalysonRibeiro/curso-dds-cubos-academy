//criar um array de tamanho 5, com numeros
//usar um loop para percorrer o array
//imprimir usando o loop

{
    const numeros = [22, 67, 2, 58, 47];

    for (let i = 0; n < numeros.length; n++) {
        console.log(numeros[i]);
    }
}

{
    const numeros = [22, 67, 2, 58, 47];
    let i = 0;
    while (i < numeros.length) {
        console.log(numeros[i]);
        i++;
    }
}